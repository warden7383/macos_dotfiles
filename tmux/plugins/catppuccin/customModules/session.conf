%hidden MODULE_NAME="sessionCustom"

# custom session module that removes the #{?client_prefix} variable
# color of the session module color only stays green instead of changing to red
# when prefix is set
set -g "@catppuccin_${MODULE_NAME}_icon" " "
set -gF "@catppuccin_${MODULE_NAME}_color" "#{E:@thm_green}"
set -g "@catppuccin_${MODULE_NAME}_text" " #{l:#S}"

source ~/.tmux/manualInstalls/plugins/catppuccin/tmux/utils/status_module.conf
