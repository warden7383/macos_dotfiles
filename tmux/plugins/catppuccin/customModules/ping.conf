%hidden MODULE_NAME="ping"

# REQUIRES: https://github.com/ayzenquwe/tmux-ping

set -g @ping_colorize false # from the tmux-ping repo

set -g "@catppuccin_${MODULE_NAME}_icon" "󰩠 "
set -gF "@catppuccin_${MODULE_NAME}_color" "#{E:@thm_blue}"
set -g "@catppuccin_${MODULE_NAME}_text" "#{l:#{ping}}"

source ~/.tmux/manualInstalls/plugins/catppuccin/tmux/utils/status_module.conf
