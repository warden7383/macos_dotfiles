# see: https://github.com/tmux-plugins/tmux-resurrect/blob/master/docs/restoring_programs.md
# for more information to saving and restoring specific programs
set -g @resurrect-processes 'btop ssh'
set -g @resurrect-capture-pane-contents 'on'
# set -g @resurrect-strategy-nvim 'session' # does not work with nvim's session manager that im using
